# Export Log entry

> characterization fixture（分割形・packet 単位ファイル `export-log/<packet-slug>.md`）。
> 単一ファイル形式の同一エントリを packet 由来の自然キーで分割したもの。中身（列）は不変。

| packet | exported_at | commit |
|---|---|---|
| append-log-規約の縫い目 | 2026-06-21T07:26:24Z | e0ad894 |
