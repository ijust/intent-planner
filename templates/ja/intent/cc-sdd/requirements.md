# Requirements (cc-sdd 投入用 下書き)

> `/intent-export-cc-sdd` が更新します。ここに生成される凝縮テキストは、cc-sdd の `/kiro-spec-init` に渡す Project Description の本文です。本体の requirements は cc-sdd が生成します（ここで完成させない）。

## Source Packet

対象 packet。

## Project Description (cc-sdd 投入本文)

cc-sdd の `/kiro-spec-init` にそのまま渡せる凝縮テキスト。(a) 誰の課題か、(b) 現状、(c) 何を変えたいか を含み、In/Out scope と守るべき invariant を明示する。情報源は対象 packet と Compass の Invariants/Anti-direction に限定し、Intent Tree 本文や他 packet を転記しない。

## Parent Intent

この requirements が支える intent（L0/L1/L2/L3 への参照）。

## Invariants

壊してはいけない振る舞い・API・データ・UX（cc-sdd が requirements/tasks に取り込む前提で明示）。

## Out of Scope

今回やらないこと。

## Open Questions

未確定事項。
