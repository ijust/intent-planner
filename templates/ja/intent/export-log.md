# Export Log

> `/intent-export-cc-sdd` が export ごとに1行追記します（1 export = 1行）。書き手は `/intent-export-cc-sdd` のみで、読み手は intent-check スクリプト・`/intent-status`・`/intent-writeback` です。書き戻し漏れの照合に使われるため、手動編集は不要です。

| packet | exported_at | commit |
|---|---|---|
