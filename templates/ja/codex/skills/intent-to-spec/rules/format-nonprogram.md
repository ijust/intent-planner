# format 写像：非プログラム成果物向け（文書・業務・研究等）

`intent-to-spec` skill の `source scope × target format` 射影における、**非プログラム成果物向けの target format（出力体裁）** の器。文書・業務プロセス・研究/意思決定など、cc-sdd も openspec も使わない経路の出力体裁をここに置く。SKILL.md は手順と報告形式のみを持ち、「どの素材をどの順で・どの見出しに配置するか」は本ルールを参照する。

> 本ファイルは seam スライスで配置した**空の器（受け皿）**であり、写像規則本体（どの素材をどう並べ替え、どの見出しへ割り当てるか）は持たない。実際の format 写像は後続の add スライスで記述する（本 seam の範囲は出力体裁の受け皿の配置までで、写像ロジックは持たせない）。

## 責務の境界（写像であって読み取り・照合ではない）

- 本ルールは **format 写像** である。素材の構成・順序・見出しへの割り当てだけを担う。射影元（intent-tree / compass / packets / steering）を読み直さず、独自のパーサ・スキーマを持たない。
- 各記述を射影元へ辿れる形にすること、inferred の標識、不変則の保持は**捏造抑制ルールの責務**であり、本ルールは行わない。canonical 由来と inferred 由来の素材は区別したまま受け取り、その区別を崩さずに配置する。
- 本ルールは**詰め方（mode）を持たない**。意図の詰め方は `intent/modes/` の責務であり（DR16）、出力体裁の器である本ルールへ混入させない。

## 基本姿勢

非プログラム成果物向けの出力体裁の基本姿勢は、後続の add スライスで確定する。

> （add スライスで写像規則を記述する。本 seam では姿勢の中身を持たせない。）

## 構成

source scope 解釈が受け渡した三層の素材をどの順で・どの見出しへ配置するかは、後続の add スライスで確定する。

> （add スライスで構成の写像規則を記述する。本 seam では構成の中身を持たせない。汎用1枚の空骨格に留め、領域別への分割や枚数・粒度の確定はここでは行わない。）

## 不変条件

- 射影元を読み直さない・変更しない（写像のみ。読み取りは source scope 解釈、書き込みは SKILL.md の派生 Write の責務）。
- canonical 由来の素材と inferred 由来の素材を混在させない（区別を保ったまま配置し、標識そのものは捏造抑制ルールに委ねる）。
- 素材に無い背景・動機・詳細を、文章を整えるために足さない。
- 出力体裁（format）の器に意図の詰め方（mode）を混入させない（DR16: 責務分離）。
