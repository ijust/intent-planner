# Check Catalog: intent-validate

The canonical source of the checks that the `intent-validate` skill applies. SKILL.md carries only the procedure and the report format; the definition of the checks references this file. To add a check, just add a row to this table (the table is the source of truth). The ID of each check is its stable identifier and is cited alongside the severity in each reported finding (this table is also the source of truth for the list and count of IDs).

## The 3 severity classes

| Severity | Definition |
|----------|------------|
| must-fix | A contradiction or consistency break that should be resolved before starting work. Proceeding to export / implementation as-is becomes a cause of architectural drift |
| recommended | A quality risk. Not severe enough to stop work immediately, but resolving it raises the deliverables' reliability as a basis for judgment |
| info | A heads-up. Action is optional, but the state should be known |

### Demotion rule

- If an intentional, reasoned deferral record (Deferred or Open Questions) exists for a missing item, report that item's severity lowered to "info".

## Check catalog

| ID | Category | Check | Condition | Severity guideline |
|----|----------|-------|-----------|--------------------|
| invariant-conflict | Contradiction | A packet's Scope/Expected Behavior conflicts with the compass Invariants | always | must-fix |
| anti-direction-violation | Contradiction | A packet's direction falls under the compass Anti-direction | always | must-fix |
| l3-intent-mismatch | Contradiction | Mismatch between the intent-tree L3 intent and the packet content (direct contradiction with an explicit L3 statement = must-fix; divergence open to interpretation = recommended) | always | must-fix/recommended |
| goal-without-packet | Coverage | A goal among the tree's L1–L3 goals that corresponds to no packet (including L4) | always | recommended |
| orphan-packet | Coverage | An orphan packet whose Parent Intent traces back to no node of the tree | always | must-fix |
| stale-questions | Coverage | Stagnation of unresolved Questions in tree/compass/packets | always | info |
| stale-assumptions | Coverage | Items remaining in the intent-tree Assumptions that have been neither promoted to canonical nor rejected | always | info |
| dependency-cycle | Consistency | `depends_on` contains a cyclic dependency A→…→A | always | must-fix |
| dependency-broken-ref | Consistency | `depends_on` references a packet_id that does not exist | always | must-fix |
| invariant-uninherited | Consistency | A compass universal invariant is not inherited into the packet `## Safety / Invariants` (silent omission, not a conflict) | always (compass Invariants filled and an active packet exists) | recommended |
| invariant-stale-vs-compass | Consistency | A compass section update date (Invariants section / Decision Rules section) is newer than the packet `updated_at` (a follow-up-lag candidate) | always (compass section update date and packet update date present) | recommended |
| decision-rule-mismatch | Consistency | The packet `## Decisions` violates the compass Decision Rules | always (compass Decision Rules filled and the packet has a `## Decisions` section) | must-fix/recommended |
| decision-rule-code-alignment | Consistency | Grep-read the code module (file name / module name) named by a Decision Rule's main statement / Context, and detect a semantic divergence between the Rule's main statement and the implementation via the AI's limited semantic matching. Matching surface = the meaning of the code implementation (separated from `decision-rule-mismatch`, whose surface = packet slot values). Output carries an "AI semantic-matching estimate" label and is a one-directional report that never asserts | always (a Decision Rule names a code module and the named target exists) | recommended |
| packet-scope-overlap | Boundary | Scope overlap / responsibility conflict between the packet files under active/ (archive/ is not read) | always | must-fix |
| decision-slot-empty | Completeness floor | Among the decision slots (④) sown in the packet's `## Decisions` section, those whose value is empty (unfilled). A reasoned `undetermined` is demoted to info by the demotion rule | a packet that has slots sown in its `## Decisions` section | recommended |
| decision-slot-unsown | Completeness floor | The `## Decisions` section exists but not a single common-core slot (none of the 8 common-core IDs of `decision-slots.md`) has been sown | a packet that has a `## Decisions` section (an old packet with no section at all is skipped as an unverified target) | recommended |
| export-draft-mismatch | Boundary | Consistency between the current export draft (the directory of the packet on the latest export-log row) and the target packet file (under active/) (mismatched transcription of Invariants, divergence from the packet definition, etc.) | always | recommended |
| requirements-smell | Quality | A requirement statement still contains vague words, subjective words, comparatives, weak words, or undefined pronouns (e.g., "appropriately", "fast", "better", "etc.", "as much as possible", an "it" with no clear referent). Detect, quote, and leave the judgment to the user (do not write the rewording back to the source) | always | recommended |
| ambiguous-deferred-phrasing | Quality | In the packet's `## Decisions` section, an unsettled verb (assume/assumed, reuse/divert, planned/TBD, tentative, etc.) that sits outside the Human-fixed / Agent-discretion partition and carries no accompanying Revisit when, hidden in finalized-sounding phrasing. Detect, quote, and leave the judgment to the user (do not write the rewording back to the source) | always (the packet has a `## Decisions` section) | info |
| trace-downstream-missing | Coverage | A packet exists for a tree L1–L3 intent, yet that packet has no downstream link (`verified-by` / verification) so it cannot be traced to verification (the verification side of downward coverage). The packet's own absence is owned by `goal-without-packet`, so do not duplicate it | always | recommended |
| trace-pre-rs-missing | Coverage | The packet frontmatter lacks the upstream link `parent_intents` key, or it is empty (the cut point of intent→requirement pre-RS). An orphan whose `parent_intents` is present but traces back to no node of the tree is owned by `orphan-packet`, so do not duplicate it | always | recommended |
| poc-experiment-missing | Normative | Any of hypothesis / falsification criteria / GO-NO-GO criteria is unrecorded in "PoC Experiment Definition" | designer-questions=on and purpose=poc | must-fix |
| l1-metric-missing | Normative | An L1 item lacks a `Measurement criteria:` line | designer-questions=on | recommended |
| walking-skeleton-missing | Normative | The "Walking Skeleton" section of plan.md is unfilled (when plan.md is filled in). **Also raise, in the same way, a section that exists but has no "Current Drift removed"** (the user's manual work that disappears once the skeleton is complete) — so that a skeleton which runs end to end through the technical layers yet replaces none of the user's work is not missed. However, stay **silent and do not treat it as unfilled** for a case where no Current Drift is written in the tree, a record stating the criterion was skipped, or an older record that predates this item (do not warn retroactively; backward compatible) | designer-questions=on | recommended |
| cross-packet-contract-coverage | Coverage | Follow each shared contract explicitly marked in Impact Analysis for new or updated packets by its identical source reference; check semantically that at least one active packet's `## Safety / Invariants` names its protection and verification, that multiple protections can coexist, and that an integration oracle can reject loss of the post-integration preservation property. Name unassigned, conflicting, and missing-oracle cases with evidence, but keep them recommendation-level and warn-only. When there is no shared contract, keep the entire check silent; exclude archive packets and make no retroactive changes | A shared contract is explicit in the Impact Analysis for new or updated packets (otherwise skip) | recommended |
| screen-sketch-missing | Normative | The "Screen Rough Reference" section is unfilled (none of a path, a link, "Not applicable", or a reasoned "none" is present) | designer-questions=on | recommended |
| designer-questions-unrecorded | Normative | designer-questions is unrecorded (skip the checks in the Normative category and announce only this row) | designer-questions unrecorded | info |
| purpose-unrecorded | Normative | purpose is unrecorded (skip the hypothesis / falsification criteria / GO-NO-GO check and announce only this row) | designer-questions=on and purpose unrecorded | info |
| coinage-suspect | Quality | First look only for project-local term-drift artifacts (`.term-drift/` at the target repository root or `skills/term-drift/SKILL.md` under the selected agent's configuration directory inside that repository). If any placement exists, stop this axis's terminology judgement: do not read `detect.md` / term-drift rules or redetermine version, hashes, the required file set, or agent-skill compatibility; guide the user to the existing installer's `--with-term-drift --dry-run` health display and to the dedicated skill only after it reports `ready`. Only when no placement exists, use `.intent/glossary.md` as the mother-set and name any term found nowhere in the ledger as a read-only "suspected coinage". The judgement is semantic (excluding proper nouns, established English terms, and legitimate new terms already given a first-mention one-line explanation), not pushed onto the mechanical check. Reflect optional row status as before: **approved** = a team common term, silent / **explicitly provisional** = even though registered, it may be raised as a candidate "term still being used without team agreement yet" / **rejected** = excluded from the silence set; when it appears, name a suspected reinvention and attach its ledger row. Rows without a status — and older 3-column ledgers with no status column — are read as provisional, but the detection output stays as before (registered = silent). An implementation whose behavior changes on an older 3-column ledger is treated as wrong. No new detection axis is added; this only extends how `coinage-suspect` reads its mother-set. It stays a candidate suggestion and stays silent when there is no suspected coinage | no term-drift placement AND `.intent/glossary.md` is present (with a placement, skip terminology judgement and guide to existing health inspection) | info |
| groundless-conclusion | Quality | Against each conclusion (intents in intent-tree, Invariants/Decision Rules in compass, etc.), check in a read-only manner whether the rationale that produced it (reasons, constraints, premises, trade-offs) is traceable in the artifacts, and name any conclusion that stands alone without traceable rationale as a "suspected groundless conclusion". The judgement is semantic (excluding self-evident intents, references to rationale already stated, and cases where another intent/Decision supplies the rationale), not pushed onto the mechanical check or the presence/absence of a required field. Attach to each finding the correctability lens (when a fact contradicting the conclusion arrives, can it be re-evaluated from the rationale?). It stays a candidate suggestion that never asserts, and stays silent when there is no suspected case. It never auto-edits canonical; supplying rationale stays an update proposal only | always | info |
| unverified-hypothesis | Quality | Against the hypotheses left in the artifacts (a packet's confident claims, the Human-fixed values in Decisions, a compass Decision/Invariant, etc.), name in a read-only manner any "hypothesis (a tentative conviction) finalized without evidence backing it". Check each hypothesis against `.intent/` (Invariants/Decision Rules/past deltas = the evidence pool), and list those whose corresponding evidence is not traceable, attaching the refutation/unverified lens. The judgement is semantic (excluding convictions whose evidence actually exists and is backed in `.intent/`), not pushed onto the mechanical check or the presence/absence of a required field. Keep its detection axis separate from `groundless-conclusion` (rationale not traceable behind a conclusion) and do not mix the findings (this axis = a hypothesis with no evidence backing it; the verification axis). **Read two kinds of evidence in particular**: **(1) the scope of what the user confirmed** — for a cause or a fix recorded as Human-fixed (settled by a person), is a matching user confirmation traceable? If the user only confirmed a symptom ("hard to use", etc.) while the AI's guess at the cause and its proposed fix were recorded as settled, raise it as a suspected settlement with no evidence behind it (INV97). **(2) the representativeness of a few examples** — for a schema or a classification promoted to canonical on the basis of a handful of reference examples, is a representativeness check traceable (which fields are common to every target versus specific to one kind, and whether the number of examples suffices), or a "provisional" marker? If neither is traceable, raise it as a suspected promotion to a canonical model without a representativeness check (INV98). Stay silent when the confirmed scope is stated, when a "provisional" marker is present, or when representativeness has been checked. It stays a candidate suggestion that never asserts, and stays silent when there is no suspected case. Keep its scope to this run's new/changed artifacts; do not make a retroactive full scan the default. When the evidence pool is empty, do not warn outright but state "cannot be judged". It never auto-edits canonical; supplying evidence stays an update proposal only | always | info |
| dangling-reference | Consistency | Check, read-only, whether the numbered cross-references inside the canonical (compass's `Anti-direction N` / `INV N` / `DR N`) have lost their target and become dangling through retirement/merge/deletion, and name any whose target is not found within the artifacts as a "suspected dangling reference". The judgement is a semantic reading in which the LLM reads the artifacts and confirms the target's existence, not pushed onto `scripts/intent-check.mjs`, grep, or a mechanical regex match (INV2/A1). Keep the target to compass's numbered references; `[[memory-slug]]` (cannot be existence-checked, in a separate repo) and a packet's `parent_intents` references are out of scope. Keep its detection axis separate from `coinage-suspect` / `groundless-conclusion` / `unverified-hypothesis` and do not mix the findings (this axis = a missing referent's existence, the presence of what a reference points to, not meaning). It stays a candidate suggestion that never asserts, and stays silent when there is no dangling reference. Keep its scope to this run's new/changed artifacts; do not make a retroactive full scan the default. It never auto-edits canonical and never re-points references | always | info |
| db-design-implementation-drift | Consistency | Report, read-only, the drift between `.intent/db-design/<slug>/db-design.md` (the intent-db-design DB design draft) and the implementation schema (migration/DDL identified by Grep). Emit, with severity, tables/constraints/indexes present in the draft but absent in the implementation, those present in the implementation but absent in the draft, and naming divergence. A full match is reported as "no drift = the draft was referenced". A range where the implementation schema cannot be fully identified is not mislabeled as "no drift" but held + reported. Fixes stay suggestions only (not written back) | the draft `.intent/db-design/<slug>/` exists (skip the axis if absent) | must-fix/recommended |
| invariant-oracle-missing | Completeness floor | Check, read-only, whether each Invariant (`INV N`) in the compass has a verification oracle attached (a yardstick by which a breach is detectable — not limited to a machine test; a procedure is fine) or, for an unattachable principled constraint, is explicitly marked "no yardstick", and name any that has **neither** as a "suspected missing oracle". An oracle that is present but reads as not making a breach detectable (tautological, unobservable) may also be surfaced as a candidate. The judgement is the LLM reading the compass semantically, not pushed onto `scripts/intent-check.mjs`, grep, or a mechanical regex match (INV2/A1; INV48). Keep its detection axis separate from `coinage-suspect` / `groundless-conclusion` / `unverified-hypothesis` / `dangling-reference` and do not mix the findings (this axis = a missing verification oracle on an Invariant). It stays a candidate suggestion that never asserts, and stays silent when nothing is missing. Keep its scope to this run's new/changed Invariants; do not make a retroactive full scan the default. It never auto-edits canonical and never attaches the oracle itself | always (compass present; one or more Invariants) | info |
| invariant-impact-reverse | Reverse lookup | Match, with grep as a **plain string match**, the changed file paths against the impact-paths attached to each Invariant in the compass (the marker for which files/paths the Invariant governs), and surface, read-only, each matched Invariant as "this change touches this Invariant (verify it with this yardstick)". Since this is a plain match needing no semantic judgement, grep is used as an aid (the convenience exception of INV48). If a surfaced Invariant has an oracle attached, present it paired (the window is aggregated into `/intent-validate`; DR72). It stays a candidate suggestion that never asserts (coarse impact-paths yield false positives), and stays silent when nothing matches. It never pre-emptively adds an index (vector/graph/GraphRAG) (A38 future candidate; INV2). It is not a gate and never stops export/implementation (INV49's warn-only) | changed file paths are provided/stated AND there are Invariants with impact-paths (skip the axis otherwise) | info |
| compass-rule-decay | Decay | Check, read-only, whether each Invariant (`INV N`) / Decision Rule (`DR N`) in the compass has become a stale rule "whose referent still exists but whose content has drifted from reality / that has long gone uncited", and name it by three types: **(a) broken premise** = the body or impact-path (A38 notation) names a file/skill/value that no longer exists / has changed (checking existence is a plain path match needing no semantic judgement, so Glob/Grep may be used as an aid = INV48 exception) / **(b) dormant** = it cannot be traced from any active packet's `parent_intents` or recent mentions in deltas/drift-log (no trace of being cited) / **(c) relic reference** = every packet referencing it is closed/archived, so no live owner remains. The judgement is an LLM reading of the compass and reference relations; it holds no numeric period/count threshold (INV2) and does not read git history (`allowed-tools` stays Read, Glob, Grep = time-axis evidence limited to in-file timestamps). Scope to Invariants + Decision Rules (Anti-direction is out of scope). Keep the detection axis separate from `dangling-reference` (referent disappeared) / `invariant-stale-vs-compass` (packet-side lag) / `stale-questions` / `stale-assumptions`; do not mix findings (this axis = the life-or-death of the canonical rule itself). It stays a candidate suggestion that never asserts, and stays silent when there is no suspicion. For an Invariant that may no longer belong there, present the three cleanup candidates: convert to a Decision, make packet-specific, or supersede; attach evidence and do not change the original | always (compass present; at least one Invariant or Decision Rule) | info |
| decision-lifecycle-relevance | Decision lifecycle | Semantically detect Preference / Heuristic to Invariant / MUST / acceptance strength promotion, a missing relevant active decision, an irrelevant or `superseded` decision mixed into the current gate, and silencing an unknown decision. Report read-only and warn-only with verbatim evidence and a candidate or `unconfirmed` temperature. Keep it separate from existing axes, keep the whole axis silent when there is no suspicion, and never change canonical data or status | always (artifacts expose decision strength or lifecycle / relevance; otherwise skip) | info |
| requirement-oracle-check | Quality | Check, read-only, whether each acceptance criterion in an export draft (`.intent/cc-sdd/<slug>/*.md` / `.intent/openspec/<slug>/*.md`) is "an observable criterion that can catch a wrong implementation". Name the weak criteria that cannot catch one (no observable input, condition, or expected result — e.g., "make it easy to use", "works appropriately") and attach a rework suggestion toward an observable form. The judgement is an LLM reading of "can this criterion reject a wrong implementation?"; it does not lean on regexes, keyword lists, or `scripts/intent-check.mjs` (INV2/A1 — same quality as the other semantic axes). Keep the detection axis separate from `requirements-smell` (surface vague wording) / `export-draft-mismatch` (draft-vs-packet consistency); do not mix findings (this axis = whether the criterion can discriminate a wrong implementation). If the same criterion surfaces on both axes, neither silences the other (different comparison surfaces). It stays a candidate suggestion that never asserts, and stays silent when there is no weak criterion. It rewrites neither the draft nor the canonical (rework stays a proposal only), and is never a gate that stops export/implementation (INV49 warn-only) | there is an export draft (a draft under `.intent/cc-sdd/` or `.intent/openspec/`; skip the axis if none) | recommended |
| starter-coverage-gap | Quality | Semantically match an active packet's Scope / Expected Behavior against the constraint starters catalog (`.intent/constraint-starters.md`'s domain index) and name, read-only, packets where **a clearly related domain's convention has been neither adopted (reflected into the packet's `## Safety / Invariants`) nor declined (recorded in the issue directory's `constraint-ledger.md`) nor deferred**, attaching which domain file (`.intent/constraint-starters/<domain>.md`) to look at as a rework suggestion. The judgement is an LLM reading of "is a convention domain that clearly relates to this packet's technical surface never considered?"; it does not lean on regexes, keyword lists, or `scripts/intent-check.mjs` (INV2/A1). Read `constraint-ledger.md`'s decision records and **do not name conventions already considered** (adopted/declined/deferred) — that is not a coverage gap (lower confidence or stay silent where the ledger is unreadable). Match only the related domains; do not brute-force against all conventions (INV57). Keep the detection axis separate from existing axes (`requirements-smell` / `requirement-oracle-check` / `invariant-uninherited`); do not mix findings (this axis = a clearly-related convention domain left unconsidered). It stays a candidate suggestion that never asserts, and stays silent when there is no clear omission. It does not auto-modify the canonical (adoption is a human folding it into the packet's Safety), and is never a gate that stops export/implementation (INV49 warn-only; A40/DR83 host ⑤) | always (there is `.intent/constraint-starters.md` and an active packet; skip the axis if none) | recommended |
| oracle-test-link-missing | Completeness floor | Cross-check a packet's acceptance oracles (the "yardstick that rejects a wrong implementation" in `## Validation`) against the verified-by in `## Verification protocol` (the corresponding real test's file path + test name), and name, read-only, **(1) a broken link** = the test the verified-by points to does not exist (deleted/renamed) / **(2) unlinked** = the oracle has no verified-by and is not marked "unlinked" either. It is the post-implementation counterpart to A38's `invariant-oracle-missing` (whether an Invariant has an oracle = pre-implementation) and works as a pair. Existence checking is a simple path match needing no semantic judgement, so Glob/Grep may assist (INV48 exception; matching down to the test name is a semantic read). The judgement is an LLM reading of the packet; it does not lean on `scripts/intent-check.mjs` or regex matching (INV2/A1). Keep the detection axis separate from `invariant-oracle-missing` (an Invariant's missing oracle) / `trace-downstream-missing` (a missing downstream link); do not mix findings (this axis = the real existence of the correspondence between an acceptance oracle and a real test). An old packet without a `## Verification protocol` section is treated as "unfilled" and is not a warning target (absence = unobserved; backward-compatible; no forced retroactive filling). One already marked "unlinked" is not blamed. It stays a candidate suggestion that never asserts, and stays silent when all correspondences are healthy. Filling in verified-by is a separate action that writeback (the writer) performs by measurement; the inspection layer does not auto-modify | always (there is an active packet; packets that have a `## Verification protocol` are the target; skip the axis if none) | recommended |
| provisional-carryover | Consistency | Cross-check an active packet's frontmatter (`state`) with `## Decisions` and name, read-only, decisions whose certainty is carried over in conflict with the progress stage: **(1) conflict with progress** = the state is verifying/done yet `未定`/`暫定` (undecided/provisional) slots remain (especially those meeting the five front-load criteria) / **(2) a satisfied revisit condition** = an undecided slot's Revisit when reads as already satisfied given the current deliverables / **(3) an unconfirmed provisional marker** = an inferred/provisional marker remains without a record of human confirmation. The aim is to close the gap where the discipline "an inference stays provisional until a human reviews it" turns into an accomplished fact by advancing to done while still held. **Do not penalize the holding of undecided items itself** (Anti-direction 300; the warning target is only the conflict with the progress stage; stay silent for packets that hold undecided items while draft/ready). The judgement is an LLM reading of the packet; it does not lean on numeric thresholds like elapsed days, `scripts/intent-check.mjs`, or regex matching (INV2/A1). Keep the detection axis separate from `ambiguous-deferred-phrasing` (the surface of an unsettled verb) / `decision-slot-empty` (an empty slot) / `compass-rule-decay` (the life or death of a compass rule) / `stale-questions` / `stale-assumptions` (stagnation of open items); do not mix findings (this axis = the conflict between a packet's decision certainty and its progress stage). An old packet without a `## Decisions` section is treated as "unfilled" and is not a warning target (backward compatible). It stays a candidate suggestion that never asserts, and stays silent when there is no conflict. Finalizing a provisional value or changing state is a separate action by a human's declaration; the inspection layer does not auto-finalize, auto-promote, or change state (Anti-direction 303; A7/INV5) | always (there is an active packet; packets that have a `## Decisions` are the target; skip the axis if none) | recommended |
| draft-content-dropped | consistency | Cross-check the export draft (`.intent/cc-sdd/<slug>/*.md`) against the deliverables the downstream spec tool generated from it (`.kiro/specs/<feature>/*.md`), and report read-only whatever the draft carried but the downstream dropped. What to look at: **(1) dropped intent references** = the Invariant / Decision Rule / parent-intent references the draft cited survive nowhere in the downstream requirements / design / tasks; **(2) dropped acceptance material** = the criteria derived from the draft's `## Acceptance Material` do not appear in the downstream requirements' acceptance criteria; **(3) dropped language discipline** = the gist of the language discipline (the fixed plainness sentence) the draft bundled at the end of `## Acceptance Material` survives nowhere in the downstream requirements / design / tasks (for older drafts without the bundle, silently skip this aspect = backward-compatible). To identify the counterpart, take the `- feature:` appended line in the export-record split file (`.intent/export-log/<packet-slug>.md`) as the **primary clue**; when absent, fall back to checking whether the packet name in the draft's `## Source Packet` appears in the downstream deliverables. When neither identifies the counterpart, say "cannot be matched" in one line and never pair them by guesswork (emit no false positives). The judgment is the LLM reading both semantically; never lean on survival-rate scoring, threshold decisions, `scripts/intent-check.mjs`, or regex matching (INV2/A1). Keep this axis separate from `export-draft-mismatch` (draft vs. packet definition) and `requirement-oracle-check` (whether the draft's acceptance criteria can reject a wrong implementation); do not mix the findings (this one is about what the downstream generation dropped). Stay at candidate suggestions without asserting, and stay silent when nothing was dropped. Never rewrite the downstream deliverables or the canonical, and never gate — do not stop the export or the implementation (INV49's warn-only) | Downstream spec deliverables exist (there are deliverables under `.kiro/specs/` and a corresponding export draft. Skip the axis when either is missing) | Recommended |
| capability-starvation | coverage | Report read-only the suspected case where a packet for a canonical capability directly tied to the intent-tree's North Star / Current Drift remains unexported and unstarted while only supporting packets are completed first. Cross-check only packet frontmatter state, the work plan / Walking Skeleton in `packets/plan.md`, export-log, and the tree's North Star / Current Drift, and present a candidate through LLM semantic judgment. Use no fixed threshold for days or completed-packet counts and no score; packet existence or a parent reference alone is not evidence that value was reached. Stay silent for a healthy plan where the value-center packet is exported or underway, or is progressing as the Walking Skeleton / highest priority. If inputs cannot be read, skip as unverified rather than claiming there is no gap. Never modify canonical, packet priority, or state automatically, and never gate | The tree has a North Star / Current Drift equivalent and there are multiple active packets (otherwise skip the axis as unverified) | info |
| outcome-provenance-missing | Quality | Only when a packet-scoped outcome record has an "Outcome learning" observation, use LLM semantic reading to check the three provenance fields `Who measured`, `When measured`, and `Where measured`. If one or several are absent, list every missing field together in that fixed order. Stay silent for an observation with all three fields and for an ordinary case with no "Outcome learning" observation. Prefer split deltas and read records absent there from the legacy single `.intent/deltas.md`; do not report a duplicated observation twice, and return the same missing-field finding for the same input. Keep the finding independent from every other check, read-only, and candidate-level; never change canonical data. This is a warn-only check that never stops recording, validation, export, or implementation | A split or legacy delta contains an "Outcome learning" observation (otherwise keep the entire check silent) | recommended |

- The condition "always" does not override the principle of unverified targets (if the target deliverable is missing or unfilled, skip that check).
- The designer-questions / purpose in the conditions refer to the values recorded in mode.md. Do not run a check whose condition is not met. When designer-questions=off is recorded, run none of the checks in the Normative category. The reader judges designer-questions first and does not consult the purpose value unless on is recorded.

## Note on the completeness-floor checks (no inference; declaration-based)

- `decision-slot-empty` / `decision-slot-unsown` carry the "completeness floor" (the cutoff line). They prevent ④ decisions-under-constraints (consistency, idempotency, error semantics, authorization, etc.) from advancing to export/implementation while blank. The canonical source for the slots is `intent-packets/rules/decision-slots.md` (it owns the categories, firing conditions, and value domain).
- **Do not infer applicability from packet content**: these checks target only the slots **actually sown** in the `## Decisions` section. They do not make inferential judgments like "this packet must involve writes, so it needs a slot" (which slot to sow is the responsibility of a human confirming it in discover's elicitation; the same discipline as not inferring `depends_on`).
- An old packet with no `## Decisions` section at all is skipped as an unverified target (no forced immediate bulk migration; the next update flow lazily completes the slots).
- A reasoned `undetermined` is demoted to "info" for `decision-slot-empty` by the demotion rule (deferral is allowed as an intentional postponement; the completeness floor is "prohibition of blanks", not "forcing immediate finalization of every item").

## Note on the dependency-soundness checks (read-only and the reference-resolution scope)

- `dependency-cycle` / `dependency-broken-ref` are **read-only** and do not modify the packet source of truth (frontmatter, etc.) at all.
- The existence check for the referenced packet_id in `dependency-broken-ref` is performed against the **full set of active+archive packet_ids** (an archived packet_id is also considered to "exist").

## Note on the smells / trace checks (read-only, minimally sufficient, no write-back)

- `requirements-smell` **only detects and quotes** vague words, subjective words, comparatives, weak words, and undefined pronouns; it does not write reworded suggestions back to the source. Its severity is "recommended" (not enough to stop work, but resolving it raises reliability as a basis for judgment).
- The trace checks (`trace-downstream-missing` / `trace-pre-rs-missing`) are **read-only** and do not write derived links or filled-in gaps back to the source (the same discipline as not inferring / auto-computing `depends_on`).
- Keep traces **minimally sufficient**: do not connect every artifact pairwise; flag only the gaps that break being able to trace "why it exists (upstream `parent_intents`), where it was realized (`realized-by`), and how it was verified (`verified-by`)". Downstream links are optional (when filled in, check whether verification can be traced).
- Boundary with the existing coverage checks: the packet's own absence is owned by `goal-without-packet`, and an orphan whose `parent_intents` is present but cannot be traced back to the tree is owned by `orphan-packet`. `trace-downstream-missing` focuses on the side where a packet exists but cannot be traced to verification, and `trace-pre-rs-missing` on the cut point where the `parent_intents` key itself is missing or empty, so no duplicate check is created.

## Note on the unsettled-verb check (read-only, strict demotion rule, no write-back)

- `ambiguous-deferred-phrasing` only detects and quotes an unsettled verb (a placeholder hidden in finalized-sounding phrasing) that sits **outside the Human-fixed / Agent-discretion partition** of the packet `## Decisions` section and carries **no accompanying Revisit when**; it does not write reworded or finalized suggestions back to the source (the same read-only pattern as `requirements-smell`). Its severity is **"info"** (do not promote it to must-fix, because the false-positive rate is high).
- **The finalized vocabulary of unsettled verbs** (limit to this list and do not overlap with the `requirements-smell` vocabulary [appropriately / fast / better / etc. / as much as possible / a bare "it", and so on]):
  - assume / assumed, reuse / divert, planned / TBD, tentative.
- **Excluding nominal-idiom false positives (required)**: even for the words above, do not flag nominal or adverbial idioms that do not signal unsettled intent. Concretely, exclude word forms used with a settled connotation such as "as expected" / "as assumed", and **limit detection to verbal usages that signal an unsettled action or intent** such as "assumed to do ~" / "to reuse / divert ~". When in doubt, do not flag it, and do not take the judgment away from the user.
- **Strict application of the demotion rule**: when a detected spot is already recorded as a deferral under Deferred / Open Questions / a reasoned undetermined slot (reason + accompanying Revisit when), suppress that finding by the demotion rule (do not re-list an unsettled item already recorded as a structural deferral). By default its severity is already "info", and it is never promoted to must-fix or recommended.
- Separation from existing axes: this axis looks at unsettled verbs (phrasing match) that sit outside the partition and have no Revisit when, while an empty slot itself is owned by `decision-slot-empty` and a contradiction with the compass Decision Rules is owned by `decision-rule-mismatch`. No duplicate check is created.

## Note on the decision-vs-code divergence check (the sole inference exception in the read-only check layer, with boundary conditions)

- `decision-rule-code-alignment` Grep-reads a code module and detects a **semantic divergence between the Rule's main statement and the implementation via the AI's limited semantic matching**, **only when a Decision Rule's main statement / Context names a code module (file name / module name)**.
- **This is the sole inference exception in the read-only check layer.** All other check axes (including the existing three [`invariant-uninherited` / `invariant-stale-vs-compass` / `decision-rule-mismatch`], `ambiguous-deferred-phrasing`, and the status / improve axes) do not infer. **Only this axis is the exception, and the exception does not spread to other axes** (the other axes keep the prior discipline of reading only the presence of a statement and the presence of a direct contradiction).
- **Boundary conditions (compensating for the reduced reproducibility of false positives; required)**:
  - The output always carries the explicit label **"AI semantic-matching estimate"** and does not assert.
  - The severity stops at **recommended**. It is never promoted to must-fix.
  - It stays a **one-directional report that prompts human confirmation** rather than the user's primary basis (it auto-fixes neither the canonical nor the code).
  - **Limited to a naming origin**: do not flag it when a Decision Rule names no module, the Rule is abstract, or no divergence can be observed ("which module to look at" originates from the Rule's naming, and the semantic matching is limited to "after the target to look at is fixed"). When in doubt, do not flag it, and do not take the judgment away from the user.
- **Separation of matching surfaces (required; avoids duplicate detection)**: the matching surface of this axis is **the meaning of the code implementation**. Because this differs from the existing `decision-rule-mismatch`, whose surface is a text match against the packet `## Decisions` slot values (no inference), even when both axes apply to the same Decision Rule it is not a duplicate detection (the former = code implementation vs. Rule, this axis = packet slot value vs. Rule).
- **Tool invariance (required)**: the intent-validate allowed-tools remain `Read, Glob, Grep` (do not add Write / Bash). Code is only Grep-read and never modified at all (INV6). The AI semantic matching is a judgment in the skill body, not a tool.

## Criteria for classifying the L3 mismatch

- Packet content that **directly contradicts an explicit statement** of the intent-tree L3 = **must-fix**
- **Divergence open to interpretation** (no explicit statement, but the direction looks off, etc.) = **recommended**
- When in doubt, lean toward recommended, attach a quotation of the evidence, and leave the judgment to the user

## Note on the compass-conformance checks (matching inheritance, staleness, and ADR divergence)

- Separation of concerns:
  - `invariant-uninherited` ≠ `invariant-conflict`: the latter detects a "conflict = contradiction", this axis detects a "silent omission". Both may apply to the same packet, but the detection viewpoint differs.
  - `decision-rule-mismatch` ≠ `l3-intent-mismatch`: the latter matches against the intent-tree L3, this axis matches against the compass Decision Rules.
- Classification criteria (`decision-rule-mismatch`, reusing the `l3-intent-mismatch` template): a direct contradiction with an explicit Decision Rules statement = must-fix; a divergence open to interpretation = recommended; when in doubt, lean toward recommended, attach a quotation of the evidence, and leave the judgment to the user.
- **Limiting the matching surface (required)**: an ADR (the 6-field long form: Context/Decision/Why/Alternatives/Consequences/Revisit when) and the packet `## Decisions` (slot value domain) are structurally asymmetric, so limit the matching surface to **"the `Decision` of a Decision Rules entry (the main statement of the option taken)" vs. "the finalized value of each slot in the packet `## Decisions`"**. Use the surrounding fields (Why/Alternatives/Consequences, etc.) as the source of evidence quotations, but not as the main axis of the contradiction judgment.
  - Matching examples: Decision Rules `Decision` = "place aggregation logic in the domain layer", the corresponding packet `## Decisions` slot finalized value = "aggregate in the UI" → direct contradiction = must-fix. `Decision` = "prefer rollback-capable slices" while the packet leans toward bulk replacement but with no explicit negation → open to interpretation = recommended.
- **Separation of axis roles (required)**: only `invariant-stale-vs-compass` carries a time axis (update-date comparison). `invariant-uninherited` (presence of inheritance) and `decision-rule-mismatch` (presence of contradiction with the ADR) are "current state" axes and are not linked to the compass section update dates. These two axes read the compass's current invariants / Decision Rules every time and flag them individually (the value of these axes is to surface "which invariant is missing in which packet").
- **Output granularity**: only the time-axis `invariant-stale-vs-compass` defaults to a **one-line count summary** (e.g., `N packets not following up after the Invariants section update / M after the Decision Rules section update`), expanding the individual packet list only on user request (to avoid crying wolf). Present it as recommended, not asserted as must-fix. The stale comparison targets only pairs where **both the relevant compass section update date and the packet `updated_at` are actually stamped (not `—`)**. `invariant-uninherited` / `decision-rule-mismatch` default to individual findings.
- No inference (required): do not infer applicability from packet content (the same discipline as the `decision-slot` checks). Keep the semantic matching to reading the presence of a statement and the presence of a direct contradiction.
- Backward compatibility: when compass `Updated (...)` is `—` / absent, the packet `updated_at` is absent, the `## Decisions` section is absent, or Invariants is unfilled, **skip that check, stating it as an unverified target with its ID** and do not assert staleness.

## Note on the DB-design drift check (draft vs. implementation schema, read-only, behavior-preserving)

- **Matching surface**: `.intent/db-design/<slug>/db-design.md` (the intent-db-design DB design draft, in the machine-diffable form of `## Table:` headings + column tables) and the implementation schema (migration/DDL/ORM schema identified by Grep). The matching units are table (heading), column (row), constraint, index, and naming.
- **Read-only, tool-invariant (mandatory)**: intent-validate's allowed-tools stay `Read, Glob, Grep` (do not add Write/Bash). The implementation schema is read only by Grep and never modified (INV6). Drift detection presents findings only; fixes are suggestions (not written back to either the draft or the implementation).
- **Behavior-preserving (mandatory)**: for cases where the draft `.intent/db-design/<slug>/` does not exist, **skip** this axis and run the other validate axes as usual (do not change at all the behavior of existing cases without a DB design).
- **Visualizing "was it referenced"**: report a full match of draft and implementation as "no drift = the draft was referenced". Divergence can be either "ignored" or "an intentional change worked out in implementation", so do not assert; enumerate it as drift and leave the classification to the writeback reason record (§ writeback link) (lossy-projection = guaranteed by visualizing the drift).
- **Fail-Safe (do not mislabel)**: a range where the implementation schema cannot be fully identified by Grep is not mislabeled as "no drift"; match only the identified range and hold + report the unidentified part (no fabrication, linked to OQ-DB5). When the drift is enormous, prioritize the higher-severity items and do not drown the user with a full enumeration (the existing reporting manner).
- **Demarcation**: this axis is the drift of "draft vs. implementation schema". The quality check of **the draft itself** (normalization breaks, etc.) is owned by intent-db-design's `db-inspect-oracle` (a separate skill / separate spec) and has a different matching surface from this axis (do not create a duplicate check).
- **No kiro coercion (A6)**: do not coerce kiro design into taking in the draft. Even if it is not referenced, quality is guaranteed by visualizing the drift (visualization, not coercion).

## Note on the unverified-hypothesis check (a hypothesis with no evidence backing it vs. a conclusion with no traceable rationale; read-only; separate axes)

- `unverified-hypothesis` names, read-only, "whether the **hypothesis (a tentative conviction)** left in the artifacts has **evidence verifying/refuting it** traceable from `.intent/` (Invariants/Decision Rules/past deltas = the evidence pool)". Like `coinage-suspect` / `groundless-conclusion`, it is a semantic judgement, a candidate suggestion, false-positive-tolerant, and non-stopping; it is not pushed onto `scripts/intent-check.mjs` or the presence/absence of a required field (INV2/A1).
- **Keep the detection axis separate from `groundless-conclusion` (A29) (required)**: `groundless-conclusion` = a **conclusion** already drawn whose producing **rationale (history)** is not traceable (the after-the-fact, preservation axis). `unverified-hypothesis` = a **hypothesis** not yet finalized that has no **evidence** backing it (the before-the-fact, verification axis). Report them as separate checks and do not mix the findings (even when both axes apply to the same spot it is not a duplicate detection — the matching surface differs: "preserving rationale" vs. "searching for evidence"). The evidence source may be shared (the rationale A29 preserves feeds into this check's evidence pool — a synergy).
- **Refutation first**: so as not to corroborate a hypothesis with cherry-picked evidence and lend authority to a wrong decision, put the evidence contradicting the conviction (refutation/unverified) first in the findings. Do not false-flag a conviction backed by evidence (being warn-only, the harm of a false positive is small, but keep the volume down).
- **Narrow the scope (noise avoidance)**: keep the target to this validate run's new/changed artifacts; do not indiscriminately re-scan the whole tree/compass retroactively (a retroactive inventory is an opt-in separate path).
- **Cold-start avoidance (Fail-Safe)**: when the evidence pool (`.intent/`) is empty, do not treat "no evidence backing = warn outright" but state "cannot be judged because there is no evidence pool" (do not mislabel it as a pass either).
- **Read-only; edits are human**: any update proposal that attaches evidence stays presentation only, and does not auto-edit the canonical (intent-tree / compass / packets). Do not let the AI fabricate evidence to retro-justify a conviction (it worsens the very drift we want to prevent = A30's premortem). Writing it is a separate action taken only after a human approves it (A7/INV5; INV37).

## Note on the dangling-reference check (dangling numbered cross-references; read-only; LLM-context; separate axes)

- `dangling-reference` names, read-only, "whether the numbered cross-references inside the canonical (compass's `Anti-direction N` / `INV N` / `DR N`) have **lost their target and become dangling** through retirement/merge/deletion". Like `coinage-suspect` / `groundless-conclusion` / `unverified-hypothesis`, it is a semantic judgement, a candidate suggestion, false-positive-tolerant, and non-stopping; it is not pushed onto `scripts/intent-check.mjs`, grep, or a mechanical regex match (INV2/A1 = the LLM reads the artifacts and confirms the target's existence).
- **Keep the detection axis separate from the existing three (required)**: `coinage-suspect` = a coinage not in the ledger / `groundless-conclusion` = a conclusion's missing rationale / `unverified-hypothesis` = a hypothesis's missing evidence / this axis `dangling-reference` = a **missing referent's existence** (the presence of what a reference points to, not meaning). Report them as separate checks and do not mix the findings (even when several axes apply to the same spot it is not a duplicate detection — the matching surface differs).
- **Narrow the target (false-positive avoidance)**: keep to compass's numbered references (`Anti-direction N` / `INV N` / `DR N`). `[[memory-slug]]` (memory is in a separate repo, so its existence cannot be checked from validate, and false positives are many) and a packet's `parent_intents` references are out of scope (a separate matter if needed). ID re-numbering (an ID swapped for a different item) is out of this axis's scope; keep to a pure missing-existence check.
- **Narrow the scope (noise avoidance)**: keep the target to this validate run's new/changed artifacts; do not indiscriminately re-scan the whole compass retroactively (a full check right after retirement is an opt-in separate path). When there is not a single numbered reference in the target, or compass is absent, skip this check and continue the others (do not error).
- **Read-only; edits are human**: naming a dangling reference stays presentation only, and does not auto-re-point references or edit the canonical (intent-tree / compass / packets). Writing/re-pointing is a separate action taken only after a human approves it (A7/INV5; INV42).
- **Not a gate**: a suspected dangling reference is a one-way report at severity "info" and does not stop export/implementation (false-positive-tolerant; the drift-watch philosophy; Anti-direction 218).

## Note on the compass-rule-decay check (the life-or-death of the canonical rule itself; read-only; LLM-context; separate axes; INV54/DR75/A41)

- `compass-rule-decay` names, read-only, whether a compass Invariant / Decision Rule itself has become a **stale rule** — its referent still exists but its content has drifted from reality (broken premise), it has long gone uncited (dormant), or only past closed packets reference it (relic reference). Like `coinage-suspect` / `groundless-conclusion` / `unverified-hypothesis` / `dangling-reference` / `invariant-oracle-missing`, it is a semantic judgement, a candidate suggestion, false-positive-tolerant, and non-stopping; it is not pushed onto `scripts/intent-check.mjs` or a mechanical regex match (INV2/A1). Only a **plain path match needing no semantic judgement** — such as "does the target of the impact-path (A38 notation) in (a) broken premise exist" — may use Glob/Grep as an aid (the convenience exception of INV48).
- **Keep the detection axis separate from the existing staleness / consistency axes (required)**: `dangling-reference` = the referent **disappeared** (dangling) / `invariant-stale-vs-compass` = the **packet side lags** compass updates (time axis; section update date vs packet update date) / `stale-questions` / `stale-assumptions` = **unresolved items lingering** / this axis `compass-rule-decay` = **the life-or-death of the canonical rule itself** (referent exists but content rotted). Report them as separate checks and do not mix findings (even when several axes apply to the same Invariant/DR it is not a duplicate — the matching surface differs). Do not confuse the names either: `invariant-stale-vs-compass` (packet-side lag) vs `compass-rule-decay` (rule decay).
- **Judgement-material constraint (holds no numeric threshold / git history; required)**: do not mechanically judge by a numeric period/count threshold ("stale if uncited for N days" violates INV2). The judgement is an LLM reading of in-file timestamps (compass `Updated (...)` tags, packet frontmatter dates, deltas / drift-log date lines) and reference relations. `allowed-tools` stays Read, Glob, Grep; even wanting time-axis evidence, do not use Bash / `git log` (the tool contract of the read-only check layer; and even if A40-(2)'s firing trace is implemented, this axis does not depend on it).
- **Target scope (limited to Invariants + Decision Rules)**: Anti-direction is out of scope (the largest population with the highest flood risk; revisit later via DR75's Revisit).
- **Output granularity (split by confidence)**: (a) broken premise = it is verified by existence and likely few, so **individual findings** (with the INV/DR number and a verbatim quote of the evidence; actionable). (b) dormant / (c) relic reference = they stay circumstantial "suspicion", so **a one-line count summary by default** (same granularity as `invariant-stale-vs-compass`); expand the individual list only on user request (do not flood with a full individual enumeration = avoid crying wolf).
- **Reach (full scan but narrowed output)**: decay is an axis that watches "what does not move / what does not appear in the diff", so a full scan is inherently required (an exception to limiting to the run's new/diff), but the output is narrowed by the granularity rule above.
- **Backward compatibility (name unverified explicitly)**: for a target whose judgement material is unreadable (no `Updated (...)` tag, no packets, empty active/, etc.), do not mislabel it as "no gap" but **name it explicitly as unverified with its ID and skip** (Fail-Safe). When compass is absent or there is not a single Invariant/DR, skip this axis and continue the others (do not error).
- **Read-only; edits are human**: naming a stale rule stays presentation only, and does not **auto-tidy, delete, or archive** the canonical (intent-compass). The cleanup is executed by a human or by `/intent-improve`'s re-alignment; do not mix detection (this axis) with the fix. Writing is a separate action taken after a human approves (A7/INV5; INV54).
- **Not a gate**: a suspected stale rule is a one-way report at severity "info" and does not stop export/implementation (false-positive-tolerant; inheriting the warn-only thinking of INV49).

## Note on decision lifecycle and relevance validation (independent axis; evidence-anchored; read-only / warn-only)

- `decision-lifecycle-relevance` independently checks only strength promotion, a missing relevant active decision, an irrelevant or `superseded` decision in the current gate, and silencing an unknown decision. Each finding includes the check ID `decision-lifecycle-relevance`, verbatim evidence with its file name, and a remediation candidate or `unconfirmed` temperature. When there is no suspicion, keep the whole axis silent.
- Keep the matching surfaces separate from `compass-rule-decay`, `decision-rule-mismatch`, and `draft-content-dropped`, and do not mix findings. Those checks respectively cover the life-or-death of a canonical rule, packet-to-Decision-Rule consistency, and content dropped from an export draft downstream; this axis covers only decision strength and lifecycle / relevance selection.
- Detection is a read-only / warn-only one-way report and does not stop export or implementation. It does not automatically change legacy four-field Decisions, legacy Compass fallback, canonical data, or status. Legacy four-field Decisions are not warned about solely because the `Revisit when` field is missing.
- When `compass-rule-decay` names an Invariant that may no longer belong there, it only presents the three cleanup candidates — convert to a Decision, make packet-specific, or supersede — with evidence and does not change the original. It never selects one automatically; an approved change follows the existing human-controlled path.

## Note on the export-draft acceptance-criterion check (can it discriminate a wrong implementation; read-only; LLM-context; separate axes; INV55/DR76/A42)

- `requirement-oracle-check` checks, read-only, whether each acceptance criterion in an export draft is "an observable criterion that can catch a wrong implementation". At the packet-slicing stage (the `/intent-packets` termination test) the discipline already demands "a criterion that can catch a wrong implementation", but there was no check for criteria that blur during the conversion into the draft. This axis catches that gap just before export. Like `coinage-suspect` / `groundless-conclusion` / `unverified-hypothesis` / `dangling-reference` / `invariant-oracle-missing` / `compass-rule-decay`, it judges semantically, suggests candidates, tolerates false positives, and never halts; it does not lean on `scripts/intent-check.mjs`, regexes, or keyword lists (INV2/A1).
- **How it judges (a semantic reading)**: read each acceptance criterion and have the LLM ask "can this criterion reject a wrong implementation that claims to satisfy it?". Name the weak criteria that cannot (no observable input, condition, or expected result; only subjective words or wishes — e.g., "make it easy to use", "works appropriately", "fast"), and attach a rework suggestion toward an observable form (input → condition → expected result). Do not flag observable criteria (those that state what goes in and what comes out).
- **Keep the axis separate from the existing quality/boundary axes (required)**: `requirements-smell` = detects and quotes the **surface wording** of a requirement (vague words, subjective words, comparatives, weak words, undefined pronouns) / `export-draft-mismatch` = the **consistency** between the draft and the packet definition (mis-transcription, divergence) / this axis `requirement-oracle-check` = whether the acceptance criterion **can discriminate a wrong implementation** (meaning). Report them as separate checks and do not mix findings. The same weak criterion surfacing on both `requirements-smell` (surface is vague) and this axis (cannot catch a wrong implementation) is normal, and neither silences the other (different comparison surfaces).
- **Do not hold the definition twice**: the source of truth for "an observable criterion that can catch a wrong implementation" lives in the `/intent-packets` termination test (the oracle in `what + constraints + oracle` = an observable acceptance criterion that can catch a wrong implementation). This axis applies that to export drafts; it does not restate the definition separately on the validate side (it refers to it).
- **Scope (export drafts only for now)**: the target is drafts under `.intent/cc-sdd/<slug>/` and `.intent/openspec/<slug>/`. Acceptance criteria in the readable artifacts that `/intent-to-spec` emits (`.intent/nl-spec/`) are out of scope for now (widen later as operation shows the need).
- **Backward compatibility (skip when no draft)**: when there is no export draft at all, skip this axis and continue the other checks (treat it as "nothing to check", not mislabel it "criteria are fine"; not an error).
- **Read-only; humans make changes**: naming the weak criteria and the rework suggestions stay proposals only; it rewrites neither the draft nor the canonical (intent-tree / intent-compass / packets) automatically. Applying them is a separate action after a human approves (A7/INV5; INV55).
- **Not a gate**: a weak-criterion finding is a one-way report at severity "recommended" and does not stop export or the downstream spec flow (false-positive-tolerant; inheriting the warn-only thinking of INV49). The severity is "recommended" because a weak criterion is not a contradiction that halts work but a quality risk whose fix raises the reliability of the draft handed downstream (on par with `requirements-smell`).

## Note on the related-convention coverage-gap check (a clearly-related convention domain left unconsidered; read-only; LLM-context; separate axes; A40/DR83 host ⑤; INV57)

- `starter-coverage-gap` checks, read-only, "is a convention domain that clearly relates to an active packet's technical surface never adopted, declined, or deferred?" Convention surfacing (firing) happens at several hosts (discover / compass / packets / db-design / at implementation start), but at every host it stays silent if the fit is weak, so an omission can pass quietly. This axis aligns the supply side (firing) with the inspection side (validate) and, just before export, makes a packet with "a clearly related convention domain never considered" noticeable.
- **Judge (an LLM reading; only clear combinations)**: read the active packet's Scope / Expected Behavior and, against the domain index of `.intent/constraint-starters.md`, name only those where "a convention domain corresponding to a technical surface this packet clearly touches (e.g., an externally-exposed API boundary / a persistent data model / concurrent updates) is never considered", attaching which domain file (`.intent/constraint-starters/<domain>.md`) to look at as a rework suggestion. To avoid false positives, **narrow to clear combinations** and do not surface marginal fits. The judgement is an LLM semantic reading; it does not lean on `scripts/intent-check.mjs`, regexes, or keyword lists (INV2/A1).
- **Read the decision ledger and stay silent on considered ones (INV57, DR84)**: read the issue directory's `constraint-ledger.md`; if a related convention is already **considered** (adopted/declined/deferred) for that packet, do not name it (it is not a coverage gap). Where the ledger is unreadable, lower confidence or lean toward silence. A declined one whose purpose/context has changed from decline time may be named as room for reconsideration (no numeric condition; INV2).
- **Match only related domains (INV57)**: do not brute-force against all conventions. From the parent catalog's domain index, look only at the domains matching the packet's technical surface (minimal-cost pull).
- **Separate the detection axis from existing quality axes (required)**: `requirements-smell` (surface vague wording) / `requirement-oracle-check` (whether the acceptance criterion can catch a wrong implementation) / `invariant-uninherited` (a compass universal invariant not inherited into the packet) vs. this axis `starter-coverage-gap` (**a clearly-related convention domain left unconsidered**). Report as separate checks and do not mix findings.
- **Backward compatibility (skip when no catalog)**: when there is no `.intent/constraint-starters.md`, skip this axis and continue the other checks (treat it as "nothing to check", not mislabel it "no coverage gap").
- **Read-only; humans make changes; not a gate**: naming and the rework suggestions stay proposals only; do **not** auto-adopt into the packet's `## Safety / Invariants` (adoption is a human's call). A one-way report at severity "recommended" that does not stop export/implementation (false-positive-tolerant; INV49 warn-only).

## Note on evidence-anchoring of findings (evidence-anchored finding; a cross-cutting discipline over all findings; read-only; LLM context; INV50/DR73/A39)

- **This is not a catalog row (a detection axis); it is a cross-cutting discipline over **how every finding is produced** by all the detection axes above plus the PBR four perspectives plus the boundary checks.** It is not added as an axis row to the catalog table, because confining it to a single axis would leave the other axes' findings unanchored and let the "groundless finding" it guards against leak from those other axes (Anti-direction 230). Its application is centralized in `SKILL.md` Step 4 (Report).
- **Anchor the evidence with a verbatim quote**: Every finding attaches a **verbatim quote** from canonical (`intent-tree.md` / `intent-compass.md` / packets) together with the file name. The quote is verbatim, not altered (no summarizing or paraphrasing).
- **A finding that cannot be backed is not raised to must-fix**: A finding that cannot be backed by a verbatim quote from canonical is not raised to "must-fix" and is capped one level down at "recommended" (OQ-eae-2). A backed finding is emitted as must-fix as usual, per the catalog's severity guidance.
- **The judgment is LLM semantic reading, not machine matching**: Whether the verbatim quote supports the finding is judged by semantic reading; it does not lean on grep, `scripts/intent-check.mjs`, or regex string-match (this avoids the surface-match trap where a semantically irrelevant quote passes on a mere string match — Anti-direction 232 / INV2 / A1). The quoted string itself is verbatim. The severity cap is a reporting discipline ("a finding that cannot be backed is not raised to must-fix"), not a machine auto-deduction.
- **Cold start (Fail-Safe)**: When canonical is empty/uncreated there is nothing to quote as backing, so state "cannot be backed by a quote" explicitly and do not raise the finding to must-fix (do not wrongly suppress all findings — same shape as `unverified-hypothesis`'s Fail-Safe).
- **Behavior-preserving; not gating**: This discipline does not break the existing axes' detection logic, output format, or verdict-summary skeleton (`must-fix N / recommended M / info K`); it only layers the annotation and severity cap across findings (preserving each axis's temperature — silence / candidate suggestion / no automatic modification / warn-only). It does not stop export/implementation (inheriting the spirit of INV49). What is borrowed is a method, not a tool (do not embed a golden dataset / regression suite / kappa / pairwise / promptfoo etc. into the product — INV51).

## Note on the dropped-draft-content check (draft → downstream deliverables; read-only; LLM-context; separate axes; INV75/DR122/A60)

- **Background**: merely "handing over" a draft lets it quietly get dropped during downstream generation. Measured in this repository (36 exports cross-checked against downstream specs, 2026-07), only about two-thirds of the intent references the drafts cited survived into the downstream requirements, and barely one in ten generated tasks carried an invariant reference. Even after fixing the mapping (the draft carries the acceptance material) and the guidance (hand the hints over per phase), the downstream generation can drop things again, so keep one eye on the decay.
- **Prefer the real link when identifying the counterpart**: the `- feature:` appended line in the export-record split file (`.intent/export-log/<packet-slug>.md`) holds the real correspondence (packet → generated spec). Only when it is missing, fall back to matching by the packet name in `## Source Packet`. When neither identifies the counterpart, say "cannot be matched" in one line and **never pair them by guesswork** (prefer silence over a false positive).
- **Keep the axis separate**: report this detection (**what the downstream deliverables dropped from the draft**) apart from `export-draft-mismatch` (draft vs. packet definition **consistency**) and `requirement-oracle-check` (whether the draft's acceptance criteria can **reject a wrong implementation**); do not mix the findings (the surfaces being cross-checked differ).
- **Cross-check survival and strength of the two JIT input layers (INV95 / DR175)**: report as Recommended a **missing confirmed material**, a **binding-strength promotion of a revalidation candidate** copied from the target packet's Agent-discretion together with the same item's `Revisit when` into MUST / SHALL / Invariant / acceptance criteria, or a **bulk injection of Tree / Compass / archive** unrelated to the task. This remains a draft-to-downstream check and stays separate from `export-draft-mismatch`'s packet-to-draft responsibility. Detect bulk injection only when structural and semantic evidence clearly shows the full content, such as reproduced heading structure, continuous excerpts, or source-wide coverage; a single necessary reference must not become a false positive. When no candidate exists, silently skip candidate survival and promotion checks.
- **Cross-check the language discipline (the check-side pair of DR151)**: include in the cross-check the language discipline (the fixed plainness sentence) the export draft bundles at the end of `## Acceptance Material`. If its gist survives anywhere downstream (any of requirements / design / tasks), do not report a drop (no verbatim match required = semantic reading). For older drafts without the bundle, silently skip this aspect and never mislabel it as "no drop" (backward-compatible).
- **Do not score it**: hold no numeric survival rate and no threshold decision (INV2). Go only as far as naming **which reference was dropped in which file**; never attach a quality score.
- **Silence and backward compatibility**: when `.kiro/specs/` is absent (cases that do not use cc-sdd, or implement directly), skip this detection and continue with the other checks (never mislabel it "nothing dropped"). When nothing was dropped, emit no output at all for this detection.
- **No automatic rewriting, no gating**: keep the naming and the make-up suggestions to a read-only report; never rewrite the downstream deliverables or the canonical. It is a one-way report at severity "Recommended" and never stops the export or the implementation (false positives assumed; INV49's warn-only).

## Responsibility boundary between the suspected-coinage check and term-drift (placement detection; one health source of truth; backward compatibility; read-only)

- **First look only for placement**: treat `.term-drift/` at the target repository root or `skills/term-drift/SKILL.md` under the selected agent's configuration directory inside that repository as a project-local term-drift placement. This is not permission to start terminology inspection; it is only a conservative presence check that switches `coinage-suspect` to guidance.
- **With a placement, perform neither terminology judgement nor health judgement**: do not read `detect.md`, run term-drift rules, judge term meaning, or determine version, hashes, the required file set, or agent-skill compatibility. Never label a placement `ready` merely because it exists. Continue the other intent checks.
- **Guide to the existing installer dry-run**: briefly tell the user to run `npx intent-planner . --agent <selected-agent> --with-term-drift --dry-run` and inspect the existing installer's health output. Proceed to the selected agent's dedicated term-drift skill only when that output says `ready`. intent-validate never launches the command or another external process itself (allowed-tools stays `Read, Glob, Grep`). The installer inspector is the one health source of truth; do not duplicate the compatible version, hashes, required file set, or agent mapping in validate-checks.
- **Only no-placement repositories keep the legacy detection**: when no project-local term-drift artifact exists, continue the usual ledger-based `coinage-suspect` with the same scope and temperature. Emit no term-drift guidance on that branch, preserving behavior for repositories without the tool.
- **No new detection axis**: placement detection is an entry branch of `coinage-suspect`; do not add a health axis or a second terminology-review axis.

## Note on the ledger term status for the suspected-coinage check (term lifecycle; backward compatibility; read-only)

- The mother-set of `coinage-suspect`, `.intent/glossary.md`, may carry per-row optional **status** (approved/provisional/rejected) and a **rewording example** (the ledger owns the schema). How to read the status: **approved** = a common term the team has agreed on = silent. **Explicitly provisional** = someone's provisional term (no team agreement yet) = even though it is in the ledger, it may be raised as a candidate "term still being used without team agreement yet" (word it differently from the suspected coinage of an unregistered term, and attach a suggestion to consider promotion to approved or a rewording). **Rejected** = a term the team decided not to adopt = excluded from the silence set; when it appears in artifacts, name it individually as "a suspected reinvention of a once-rejected term", attaching the rejecting ledger row (and its rewording example if present) (this is where keeping rejected terms in the ledger instead of deleting them pays off = preventing reinvention).
- **Backward compatibility (older 3-column ledgers keep their behavior; required)**: rows without a status — and older 3-column ledgers with no status column — are read as "provisional" (= they cannot claim team approval; promotion to approved happens only through per-term individual human approval). However, **the detection output stays as before** (registered = silent; no new notification either). Only explicitly provisional terms are raised as candidates because a term someone has declared "not agreed yet" is worth telling about, whereas showering notifications on existing ledgers that carry no status (every already-distributed project) would provoke bulk promotion just to silence the check (approval laundering). An implementation whose behavior changes on an older 3-column ledger is treated as wrong.
- **No new detection axis**: reading the status is an extension of how this axis (`coinage-suspect`) reads its mother-set; the check ID, category, severity (info), and temperature (semantic judgement; candidate suggestion; silence; no gate; no auto-editing) stay as they are. Registering a term, promoting its status, and applying a rewording are separate actions approved by a human one term at a time (bulk registration, approval that skips the check, and promotion without approval do not count as registration), and the check layer never rewrites the ledger.

## Note on the boundary checks (target selection for the export drafts)

- The export drafts (`.intent/cc-sdd/<slug>/*.md`) **coexist per packet**. The target of the export-draft-consistency boundary check is limited to the directory of the packet on the latest row of `.intent/export-log.md`. Drafts of past packets coexist by design, so their existence itself is not treated as a violation.

## Handling of unverified targets (the verifiable-scope principle)

1. If a deliverable subject to verification is missing or unfilled, skip the checks that require that deliverable.
2. Run the remaining checks within the verifiable scope (do not abort the whole run).
3. Include "unverified targets" in the report, stating the skipped checks and the reason (which file is missing / unfilled).
4. Example: `.intent/packets/` is absent (or active/ is empty) → skip the packet-dependent checks across contradiction/coverage/boundary, and run only the checks possible with the tree/compass alone (e.g., stagnation of unresolved Questions).
