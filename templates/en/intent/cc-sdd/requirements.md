# Requirements (draft for cc-sdd input)

> Updated by `/intent-export-cc-sdd`. The condensed text generated here is the body of the Project Description passed to cc-sdd's `/kiro-spec-init`. The main requirements are generated by cc-sdd (do not complete them here).

## Source Packet

The target packet.

## Project Description (cc-sdd input body)

Condensed text that can be passed to cc-sdd's `/kiro-spec-init` as is. It includes (a) whose problem it is, (b) the current state, (c) what you want to change, and makes the In/Out scope and the invariants to protect explicit. Limit the information source to the target packet and the Compass's Invariants/Anti-direction; do not transcribe the Intent Tree body or other packets.

## Parent Intent

The intent this requirements supports (a reference to L0/L1/L2/L3).

## Invariants

Behavior / API / data / UX that must not be broken (made explicit on the premise that cc-sdd incorporates them into requirements/tasks).

## Out of Scope

What will not be done this time.

## Open Questions

Undetermined items.
